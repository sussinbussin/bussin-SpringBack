type Query {
    allUsers: [User]
}

scalar Date

type User {
    uuid: ID!
    nric: String!
    name: String!
    address: String
    dob: Date
    mobile: String
    email: String
    isDriver: Boolean
}